import React from "react";
import { shallow } from "enzyme";
import Task from "./task.component";
it("should be", () => {
  //   const mockProps = {};
  //   expect(shallow(<Task task={mockProps} />)).toMatchSnapshot();
});
